version: '2.1'

services:

  docker-volume-ipfs:
    build:
      context: .
      dockerfile: Dockerfile.${ARCH}
    image: sofwerx/docker-volume-ipfs:${ARCH}
    environment:
      ARCH: ${ARCH}
    volumes:
      - /:/chroot

